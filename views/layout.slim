
html lang="en"
    head
        meta charset="UTF-8"/
        meta http-equiv="X-UA-Compatible" content="IE=edge"/
        meta name="viewport" content="width=device-width, initial-scale=1.0"/
        title Collectoneer
        link rel="stylesheet" href="/style.css"/
    body 
        
        header.main_header Collectoneer
        nav 
            a href="/store" Store
            a href="/users" Users
            - if session[:user_id] != nil
                a href="/inventory" Inventory

                a href="/trades" Trades


                a href="/logout" Logout
                a href="/balance" Coins: #{current_user.coins }
            - else      
                a href="/login" Login/Register
        
        - if session[:user_id] != nil
            - if User.from_id(session[:user_id]).rank >= 1
                nav.admin_bar
                    h2 Admin options:
                    a href="/admin/item/new" Upload new item
                    a href="/admin/item/remove" Remove item
                    a href="/admin/user/edit" Set coins
        == yield

